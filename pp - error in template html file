In template /root/pp/pp/templates/base/navigation.html, error at line 36

Reverse for 'pp.logs.views.consoleLogs' with arguments '()' and keyword arguments '{}' not found. 0 pattern(s) tried: []

before:

36	        <a href="{% url "pp.logs.views.consoleLogs" %}" class="console_logs">CLogs</a>

after:

      <a href="{% url "pp.logs.views.consoleLogs" %}" class="console_logs">CLogs</a>

In template /root/pp/pp/templates/account/portal_dashboard.html, error at line 62, 
Reverse for 'createQuickCompany' with arguments '()' and keyword arguments '{}' not found. 0 pattern(s) tried: [],
fix


62								<a href="{% url "createQuickCompany" %}"><img title="Add Quick Company" src="{{ MEDIA_URL }}e911/img/icons/add_many.png"/></a>

createQuickCompany is not defined in account/urls.py. Let me search for where it might be defined:

Found it! The view exists in urls.py but isn't named. Let me check and fix it:

urls.py

The URL pattern exists but doesn't have a name. Let me add it:

Edited
urls.py+1-1
Fixed. Added the name createQuickCompany to the URL pattern in business/urls.py. 
The template can now reverse this URL properly.

after change, still get same error

The name was added correctly. The issue is that business.urls is included with (r'^business/', include('business.urls')) using the old tuple syntax instead of url(). In Django, when using tuple syntax for includes, URL names from the included URLconf are registered directly. But since other patterns in urls.py have names that work, the name should be available.

However, looking at the include statement - it's using tuple syntax while other patterns use url(). Let me check if that's causing issues. Also, I need to restart Django for changes to take effect. Let me verify the server was restarted:

 Run bash command?



    url(r'company/create/quick/$', 'createQuickCompany', name='createQuickCompany'),

